---
import '../styles/main.css'
import i18next,{changeLanguage} from "i18next";
import { HeadHrefLangs } from "astro-i18next/components";
import { getLanguageFromURL } from '../util';
import { rtlLanguages } from '../language';
import Header from '../components/Home/Header.astro';
export interface Props {
	title: string;
}

const { title } = Astro.props;
// const url = new URL(Astro.request.url);
// const lang = getLanguageFromURL(url.pathname);
// const direction = rtlLanguages.has(lang) ? 'rtl' : 'ltr';
// console.log(direction); 
// const currentPage = url.pathname;
// console.log(currentPage);

// const direction = i18next.language === 'ar' ? "rtl" : "ltr"
// const currentLanguage = i18next.language;
// console.log(currentLanguage);

// changeLanguage()
---

<!DOCTYPE html>
<html  lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@algolia/algoliasearch-netlify-frontend@1/dist/algoliasearchNetlify.css" />
		<HeadHrefLangs />
	</head>
	<body>
		<Header  />
		<slot />		
		<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/@algolia/algoliasearch-netlify-frontend@1/dist/algoliasearchNetlify.js"></script>
		<script type="text/javascript">
		algoliasearchNetlify({
			appId: 'MRH8YF5JM3',
			apiKey: 'bb6ed324adc765cbef1c14491608b7e9',
			siteId: 'a8f9ba96-6cf9-40bd-8d80-b85e9535fe73',
			branch: 'algo-test',
			selector: 'div#search',
		});
		</script>
	</body>
</html>

