---
import i18next, { changeLanguage } from "i18next";

export const getStaticPaths = () => Object.keys(i18next.languages).map((lang) => ({ params: { lang } }));

const { lang } = Astro.params;
---
<meta http-equiv="refresh" content={`0;url=/${lang}/`} />