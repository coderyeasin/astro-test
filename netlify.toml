[build]
  command = "yarn build"
  publish = "dist/"

[[plugins]]
package = "@algolia/netlify-plugin-crawler"
  [plugins.inputs]
  branches = ['main', 'draft/jolly-neumann', 'en', 'ar', 'final_design']
  renderJavaScript = true
